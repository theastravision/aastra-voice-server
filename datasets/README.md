# Training datasets (not shipped in git)

Audio corpora are **downloaded separately** — see [docs/DATASETS_AND_TRAINING.md](../docs/DATASETS_AND_TRAINING.md).

- Catalog: `oss_manifest.json`
- Download helper: `bash scripts/download-datasets.sh --list`
- Default download dir: `data/datasets/` (gitignored)
